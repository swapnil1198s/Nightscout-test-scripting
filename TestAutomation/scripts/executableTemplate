var src = require('../project/src/%');
var expect = require('chai').expect;

describe('%(); return %', function(){
    context('%', function(){
        it('%', function(){
            expect(src.%(%)).to.equal(%)
        })
    })
})